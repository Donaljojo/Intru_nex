{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
  <h1>Welcome to IntruNex Dashboard</h1>

  {% if app.user %}
    <p>You are logged in as {{ app.user.email }}</p>
  {% else %}
    <p>You are not logged in.</p>
  {% endif %}
  <a href="{{ path('asset_list') }}" class="btn btn-primary">
    Manage Assets
</a>

{% endblock %}



